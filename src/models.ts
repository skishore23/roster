/** The current quality-first OpenAI default used when no deployment override is set. */
export const DEFAULT_OPENAI_MODEL = "gpt-5.6-sol";

/** Low-cost semantic model used for ordinary Coding workspace conversation. */
export const DEFAULT_CODING_CONVERSATION_MODEL = "gpt-5.6-luna";
