@AGENTS.md

Claude Code participates as a Roster workspace-node runtime when a
`claude-code` adapter is configured. Keep Claude's conversation and filesystem
inside its runtime placement; publish only bounded findings, evidence,
proposals, and artifact references to the Roster workspace.
